apiVersion: v1
kind: Service
metadata:
  name: beatwave-mongodb
spec:
  type: ClusterIP
  clusterIP: None 
  selector:
    app: beatwave-mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
      name: mongodb

