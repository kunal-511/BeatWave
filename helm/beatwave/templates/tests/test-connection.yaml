apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "beatwave.fullname" . }}-test-connection"
  labels:
    {{- include "beatwave.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "beatwave.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
